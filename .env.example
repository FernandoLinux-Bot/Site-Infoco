# Copie este arquivo para `.env` (que é ignorado pelo git) e preencha os valores.
#
# Atualmente NENHUMA variável é obrigatória — o site funciona sem .env.
#
# API_KEY: chave do Google Gemini. Era usada pela antiga página de Notícias
# (hoje uma vitrine do Instagram). Mantida aqui apenas caso a integração
# com IA seja reintroduzida. Sem ela, o site funciona normalmente.
# API_KEY=
